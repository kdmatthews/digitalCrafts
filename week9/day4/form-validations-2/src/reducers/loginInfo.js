const initialState = {
    userName: "",
    password: "",
   
}

const LoginInfo = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default LoginInfo;