const initialState ={
   name: "mandarin",
   name: "lime"
}

const ShopInfo = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default ShopInfo;