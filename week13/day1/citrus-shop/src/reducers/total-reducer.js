import { TOTAL_CART } from "../action-types/total-types";

const initialState = [];

const TotalInfo = (state=initialState, action) => {
    switch(action.type){
        case TOTAL_CART:

        default:
            return
    }
}

export default TotalInfo;
