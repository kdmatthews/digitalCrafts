const initialState = []

const Matchups = (state=initialState, action) => {
    switch(action.type){
        case "":
            return 
        default:
            return state;
    }
}

export default Matchups;